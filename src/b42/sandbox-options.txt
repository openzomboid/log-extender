VERSION = 1,

option LogExtender.PlayerLogs {
  type        = boolean,
  default     = true,
  page        = LogExtender,
  translation = PlayerLogs
}

option LogExtender.PlayerTickLogs {
  type        = boolean,
  default     = true,
  page        = LogExtender,
  translation = PlayerTickLogs
}

option LogExtender.VehicleLogs {
  type        = boolean,
  default     = true,
  page        = LogExtender,
  translation = VehicleLogs
}

option LogExtender.VehicleAdminTools {
  type        = boolean,
  default     = true,
  page        = LogExtender,
  translation = VehicleAdminTools
}

option LogExtender.BrushToolLogs {
  type        = boolean,
  default     = true,
  page        = LogExtender,
  translation = BrushToolLogs
}

option LogExtender.SafehouseLogs {
  type        = boolean,
  default     = true,
  page        = LogExtender,
  translation = SafehouseLogs
}

option LogExtender.SafehouseAdminTools {
  type        = boolean,
  default     = true,
  page        = LogExtender,
  translation = SafehouseAdminTools
}

option LogExtender.CraftLogs {
  type        = boolean,
  default     = true,
  page        = LogExtender,
  translation = CraftLogs
}

option LogExtender.AnimalLogs {
  type        = boolean,
  default     = true,
  page        = LogExtender,
  translation = AnimalLogs
}

option LogExtender.SafehousesDumper {
  type        = enum,
  default     = 3,
  numValues   = 3,
  page        = LogExtender,
  translation = SafehousesDumper,
}

option LogExtender.FactionsDumper {
  type        = enum,
  default     = 3,
  numValues   = 3,
  page        = LogExtender,
  translation = FactionsDumper,
}
